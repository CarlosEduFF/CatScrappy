# Interface de menus no terminal
questionary>=2.0

# Navegador headless (necessário só para o Goyabu, que tem proteção anti-bot).
# Após instalar, rode: playwright install chromium
playwright>=1.40

# Download de streams HLS (.m3u8)
yt-dlp>=2024.4.9

# Geração de PDF dos capítulos de mangá
Pillow>=10.0
